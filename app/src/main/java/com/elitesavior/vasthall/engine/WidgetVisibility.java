package com.elitesavior.vasthall.engine;

/**
 * UMG-lite visibility. Unreal analog: {@code ESlateVisibility} reduced to
 * the three states a GameMode needs before a designer exists.
 */
public enum WidgetVisibility {
    VISIBLE,
    HIDDEN,
    COLLAPSED
}
